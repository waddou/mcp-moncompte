{
  "name": "mcp-moncompte",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Serveur MCP (lecture seule) exposant le catalogue de notices de moncompte.org aux agents IA, sur Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.0.80",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.36.0"
  }
}
